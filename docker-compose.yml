services:
  backend:
    build: ./eat-this-back
    ports:
      - "8000:8000"
    env_file:
      - ./eat-this-back/.env
    environment:
      APP_MODE: ${APP_MODE:-open}
    volumes:
      - hf-cache:/models

volumes:
  hf-cache:
